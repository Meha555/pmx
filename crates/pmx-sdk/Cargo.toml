[package]
name = "pmx-sdk"
version = "0.1.0"
edition = "2024"
description = "Shared types, traits and ABI for PMX plugin modules"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
